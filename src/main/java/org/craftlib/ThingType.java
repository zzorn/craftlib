package org.craftlib;

/**
 * A type of thing, e.g. sword, table, etc.
 * Each instance or design for an instance may have different parameters e.g. for sizes or other things.
 */
public interface ThingType {
}
